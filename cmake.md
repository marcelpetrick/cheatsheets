# QtCreator Cmake project - no direct parameter-UI-option
Therefore set this to the build environment:
```
CMAKE_BUILD_PARALLEL_LEVEL=12
```

Impact for a tiny project: before rebuild took 32s, then 6s.
